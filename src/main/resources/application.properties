spring.application.name=OnlyKick
server.address=0.0.0.0

server.port=${PORT:8080}

# --- CONEXIÓN A BASE DE DATOS
spring.datasource.url=jdbc:postgresql://ep-old-mode-a4oz70yk-pooler.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_qEf12sMHrAGP
spring.datasource.driver-class-name=org.postgresql.Driver

# --- OPTIMIZACIÓN DE CONEXIONES (HikariCP) ---
# Configuración recomendada para Render/Neon
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.leak-detection-threshold=60000

# --- HIBERNATE / JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# --- SWAGGER ---
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/doc/swagger-ui.html

# --- OTROS ---
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

# --- SEGURIDAD ---
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration,org.springframework.boot.actuate.autoconfigure.security.servlet.ManagementWebSecurityAutoConfiguration

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always